## 📖 Documentación de la API (Swagger)

El proyecto cuenta con documentación interactiva generada con Swagger (OpenAPI), donde podrás visualizar todos los endpoints, modelos de datos y realizar pruebas directamente desde el navegador.

### 🚀 Acceso a la documentación

Dependiendo de cómo estés ejecutando el proyecto, la URL es la siguiente:

| Entorno | URL de Documentación |
| :--- | :--- |
| **Local (npm run start:dev)** | [http://localhost:3000/api/docs](http://localhost:3000/api/docs) |
| **Docker (docker-compose up)** | [http://localhost:3000/api/docs](http://localhost:3000/api/docs) |

> **Nota:** Si has modificado el puerto en el archivo `.env` o en el `docker-compose.yml`, asegúrate de usar el puerto configurado (por defecto es 3000).